# these commands are needed to operate on the pi
#   main user, surrogate through the homeassistant user,
#   and establish the root permissions needed for git commits

sudo -u homeassistant -H -s
cd /srv/homeassistant
python3 -m venv .
source bin/activate